<%= form_for(current_user.cat_followed_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :following_id, @category.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>
